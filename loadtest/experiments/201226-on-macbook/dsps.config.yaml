logging:
  category:
    http: WARN

storages:
  redis1:
    redis:
      singleNode: "127.0.0.1:${REDIS1_PORT}"
      connection:
        max: 2048
        min: 2048
  redis2:
    redis:
      singleNode: "127.0.0.1:${REDIS2_PORT}"
      connection:
        max: 2048
        min: 2048
